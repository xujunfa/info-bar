import CoreGraphics

enum QuotaLayoutMetrics {
    static let statusWidth: CGFloat = 104
    static let statusHeight: CGFloat = 22

    static let iconX: CGFloat = 2
    static let iconSize: CGFloat = 16
    static let textX: CGFloat = iconX + iconSize + 4
}
